from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='How to build a neural networks from scratch with numpy',
    author='Hesbon',
    license='MIT',
)
